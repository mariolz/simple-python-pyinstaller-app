 pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'git clone https://github.com/mariolz/simple-python-pyinstaller-app.git'
            }
        }
    }
}
